# BrickBrain MCP Server Environment Variables Example
# Copy this file to .env and fill in your actual values

# Databricks workspace configuration
DATABRICKS_HOST=https://dbc-xxxxxxxx-xxxx.cloud.databricks.com
DATABRICKS_TOKEN=dapi1234567890abcdef

# Knowledge Agent endpoint name (check your Databricks workspace)
ENDPOINT_NAME=ka-xxxxxxxx-endpoint

# Server configuration
SERVER_NAME=brickbrain-mcp

# Usage Instructions:
# 1. Copy this file: cp env.example .env
# 2. Replace the example values with your actual Databricks credentials
# 3. Get your DATABRICKS_HOST from your workspace URL
# 4. Generate DATABRICKS_TOKEN from User Settings → Access Tokens
# 5. Find ENDPOINT_NAME in your Databricks Knowledge Agent setup

# Security Notes:
# - Never commit the .env file to version control
# - The .env file is already in .gitignore
# - Use workspace-scoped tokens when possible
# - Rotate tokens regularly
