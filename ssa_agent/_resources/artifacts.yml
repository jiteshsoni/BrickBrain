resources: 
  # TODO: update once Knowledge Assistant brick is configurable via SDK
  # model_serving_endpoints:
  #   brickbrain_model_serving_endpoint:
  #     name: ${var.uc_catalog}-${var.schema}-${var.registered_model}
  #     permission: CAN_QUERY
  # experiment: 


  apps:
    # Slackbot application
    brickbrain_slackbot:
      name: ${bundle.target}-brickbrain-slackbot
      description: "BrickBrain Slackbot"
      source_code_path: ${workspace.file_path}/slackbot/app
      resources:
        # TODO: update this with the correct configuration.
        # - name: serving-endpoint
        #   serving_endpoint:
        #     name: ${ka_endpoint_name}
        #     permission: CAN_QUERY
        #   database: 
        #     name: ${lakebase_instance}
        #   secret: 
        #     name: ${databricks_secret_scope}
          

    # Chat UI application  
    brickbrain_chatapp:
      name: ${bundle.target}-brickbrain-chatapp
      description: "BrickBrain Streamlit chat interface"
      source_code_path: ${workspace.file_path}/chatapp/app
